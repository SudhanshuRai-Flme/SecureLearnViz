3. **Firewall Rule Simulator**  
   - **File**: `client/src/components/FirewallRuleSimulator.tsx`  
   - **UI**:  
     - Rule editor: list/edit rules like `ALLOW 80/TCP`, `DENY 22/TCP`.  
     - Packet tester: inputs for port and protocol.  
     - “Simulate” button that animates a packet path (Framer Motion) and shows ✅ or ❌.  
     - Styled rule list, input fields, and result card with Tailwind.  

4. **JWT Visualizer**  
   - **File**: `client/src/components/JWTVisualizer.tsx`  
   - **UI**:  
     - Input box for a JWT string.  
     - On submit, decode and display header, payload, signature in separate cards.  
     - “Tamper Payload” button to let the user mutate the payload and show “Signature Invalid ❌”.  
     - Framer Motion animations for showing/hiding sections.  
     - Tailwind color‑coded cards and responsive layout.  

5. **Packet Dissector (Mini Wireshark)**  
   - **File**: `client/src/components/PacketDissector.tsx`  
   - **UI**:  
     - Preload a mock packet (Ethernet > IP > TCP > Application).  
     - Display each layer as a collapsible card showing key fields.  
     - Framer Motion expand/collapse animations.  
     - Tailwind styling with distinct colors for each layer.  

---

**Implementation Details (for all components)**:  
- Use **React Hooks** (`useState`, `useEffect`) to manage state.  
- Use **Framer Motion** for all animations.  
- Style everything with **Tailwind CSS**, ensuring mobile responsiveness.  
- Type all state and props in **TypeScript**.  

**Integration**:  
- Export each component from its file.  
- In `client/src/pages/HomePage.tsx`, add under new `<section>` headings:  
  - “Cross‑Site Scripting (XSS) Visualizer”  
  - “TLS Handshake Visualizer”  
  - “Firewall Rule Simulator”  
  - “JWT Visualizer”  
  - “Packet Dissector”  
- Import each component at the top.  

**Deliverables**:  
Paste into your agent’s response **only**:  
1. Full contents of the five new component files (`*.tsx`).  
2. The updated import statements and JSX snippets added to `HomePage.tsx`.  
3. No other file modifications.  