
**Scope of Work**  
- Update global layout (header, navigation, footer) for mobile  
- Ensure each visualization component adapts to small screens  
- Optimize spacing, font sizes, and stacking order for phone viewports  

---

**1. Global Layout Adjustments**  
- **Header & MainNavigation** (`client/src/components/Header.tsx`, `MainNavigation.tsx`):  
  - Collapse nav links into a hamburger menu below `sm` breakpoint.  
  - Use Tailwind’s `sm:hidden`/`sm:flex` utilities.  
  - Add `px-4 py-2` on mobile, increase tap targets via `py-3`.

- **Footer** (`Footer.tsx`):  
  - Stack items (`flex-col`) on mobile, center content.  
  - Use `text-sm` for smaller screens.

---

**2. Page Container & Section Wrappers**  
- In `App.tsx` or global CSS:  
  - Wrap main content in `<div className="max-w-screen-md mx-auto px-4 sm:px-6">`.  
  - Ensure headings (`<h2>`, `<h3>`) use `text-xl sm:text-2xl` or larger for clarity.

- For each `<section>`:  
  - Add `mb-8` and `space-y-4 sm:space-y-6` to control vertical rhythm.

---

**3. Visualization Component Responsiveness**  
For each component file under `client/src/components/` (e.g., `XSSAttackVisualizer.tsx`, `PageProtectionVisualizer.tsx`, etc.):

- **Container**:  
  - Wrap main wrapper in `<div className="w-full flex flex-col sm:flex-row items-center sm:items-start space-y-4 sm:space-y-0 sm:space-x-6">`.
- **Inputs & Controls**:  
  - Stack vertically on mobile: `<div className="flex flex-col space-y-2 sm:flex-row sm:space-y-0 sm:space-x-4">`.
  - Use `w-full sm:w-auto` on input fields and buttons.
- **Visual Canvas / Preview Box**:  
  - Set `w-full h-auto` for mobile, cap height with `max-h-64 sm:max-h-80`.
  - Use `overflow-auto` for any scrollable content.
- **Text & Tooltips**:  
  - Use `text-sm` on mobile and `text-base sm:text-lg` for larger screens.
  - Ensure tooltips don’t overflow: wrap with `<div className="break-words">`.

---

**4. Animations & Motion Containers**  
- For any `<motion.div>` or stepper wrappers, add `initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.4 }}` for mobile, and increase to `0.6` for screens ≥ `sm`.
- Ensure motion elements use responsive props where needed (e.g., width: `vw` vs. `w-full`).

---

**5. Integration & Deliverables**  
- Provide only the updated JSX/TSX files with Tailwind class changes.  
- List each file changed at the top of the response.  
- Do not modify any business logic, only Tailwind class names and responsive wrappers.